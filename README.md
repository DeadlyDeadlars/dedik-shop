# 🚀 DeadlyVDS - Telegram Shop Bot

Современный Telegram-бот для продажи VDS/VPS серверов с интеграцией CryptoBot для приема платежей в криптовалюте.

## ✨ Возможности

- 🛒 **Каталог серверов** - удобный выбор тарифов по локациям
- 💰 **Криптоплатежи** - прием оплаты через CryptoBot (USDT, BTC, ETH и др.)
- 👥 **Реферальная система** - привлекайте новых клиентов и получайте бонусы
- 🎁 **Промокоды** - система скидок и акций
- 📊 **Админ-панель** - управление заказами и статистика
- 💾 **База данных** - поддержка SQLite и PostgreSQL

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd python-bot
pip install -r requirements.txt
```

### 2. Настройка окружения

Создайте файл `.env` в папке `python-bot`:

```env
# Обязательные параметры
TELEGRAM_BOT_TOKEN=your_bot_token_here
CRYPTOBOT_TOKEN=your_cryptobot_token_here
DATABASE_URL=sqlite:///shop.sqlite3

# Опциональные параметры
CRYPTOBOT_WEBHOOK_SECRET=your_webhook_secret
ADMIN_IDS=123456789,987654321
RUB_USDT_RATE=100
```

### 3. Запуск бота

```bash
python3 -m bot.main
```

Бот запустится в режиме polling и создаст локальный сервер для webhook'ов CryptoBot на порту 8080.

## ⚙️ Настройка

### Получение токенов

1. **Telegram Bot Token**: 
   - Создайте бота через [@BotFather](https://t.me/BotFather)
   - Получите токен командой `/newbot`

2. **CryptoBot Token**:
   - Перейдите в [@CryptoBot](https://t.me/CryptoBot)
   - Создайте приложение и получите API токен

### База данных

Поддерживаются два типа БД:

- **SQLite** (по умолчанию): `DATABASE_URL=sqlite:///shop.sqlite3`
- **PostgreSQL**: `DATABASE_URL=postgresql://user:pass@localhost:5432/dbname`

## 🎮 Команды администратора

| Команда | Описание |
|---------|----------|
| `/admin` | Открыть админ-панель |
| `/seed` | Заполнить каталог предустановленными серверами |
| `/add_server Локация\|Характеристики\|Цена` | Добавить отдельный сервер |
| `/add_promo код скидка мин_сумма макс_использований` | Создать промокод |
| `/del_promo код` | Удалить промокод |
| `/init_db` | Инициализировать базу данных |

### Примеры команд

```bash
# Добавить сервер
/add_server Россия|4 Gb RAM / 2 Core CPU / SSD 40 Gb|650

# Создать промокод на 20% скидку
/add_promo SALE20 20 500 100

# Удалить промокод
/del_promo SALE20
```

## 👥 Пользовательские функции

- **🛒 Каталог серверов** - просмотр и заказ серверов
- **📦 Мои заказы** - история заказов и их статусы
- **🎁 Промокоды** - активация скидочных кодов
- **👥 Реферальная система** - получение реферальных ссылок и бонусов
- **👤 Профиль** - информация о пользователе и балансе

## 🔧 Архитектура

```
python-bot/
├── bot/
│   ├── main.py          # Точка входа
│   ├── handlers.py      # Обработчики команд и сообщений
│   ├── models.py        # Модели данных (Users, Orders, Tariffs)
│   ├── db.py           # Работа с базой данных
│   ├── config.py       # Конфигурация
│   ├── cryptobot.py    # Интеграция с CryptoBot API
│   └── webhook.py      # Webhook сервер для платежей
├── requirements.txt     # Зависимости Python
└── .env                # Переменные окружения
```

## 🛠 Разработка

### Структура базы данных

- `users` - пользователи и их данные
- `tariffs` - каталог серверов
- `orders` - заказы пользователей
- `promocodes` - промокоды и скидки
- `referral_rewards` - реферальные награды
- `user_active_promocodes` - активированные промокоды
- `settings` - настройки системы

### Добавление новых функций

1. Добавьте обработчики в `handlers.py`
2. Создайте модели в `models.py` при необходимости
3. Обновите схему БД в `db.py`

## 📝 Лицензия

Этот проект создан для демонстрационных целей. Используйте на свой страх и риск.

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте правильность настройки `.env`
2. Убедитесь, что все токены действительны
3. Проверьте логи бота на наличие ошибок
4. Убедитесь, что база данных доступна

---

**Создано с ❤️ для автоматизации продаж VDS/VPS серверов**
